# ─────────────────────────────────────────────────────────────
# Non-secret runtime configuration for flutter_app_itse500.
# Do NOT put real API keys or secrets in this file — it is bundled
# into the app and tracked in git. Provider API keys are entered by
# the user at runtime and stored via flutter_secure_storage.
# See .env.example for the full list of supported variables.
# ─────────────────────────────────────────────────────────────

# Backend API base URLs (primary + dev fallbacks).
# TODO(Phase 1b): switch PRIMARY_API_BASE to the HTTPS VPS domain once Traefik is live.
PRIMARY_API_BASE=http://www.itse500-ok.ly/api/v1/
SECONDARY_API_BASE=http://127.0.0.1:8000/api/v1/
TERTIARY_API_BASE=http://192.168.22.99:8000/api/v1/

# Local LM Studio endpoints
LMSTUDIO_BASE=http://localhost:1234/v1/
LMSTUDIO_EMULATOR_BASE=http://10.0.2.2:1234/v1/

# Provider API base URLs (public, non-secret)
GOOGLE_GEMINI_BASE=https://generativelanguage.googleapis.com/v1beta/
OPENAI_BASE=https://api.openai.com/v1/
OPENROUTER_BASE=https://openrouter.ai/api/v1/
